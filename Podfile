# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'RatingSystemDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for RatingSystemDemo
  pod 'Cosmos'
  target 'RatingSystemDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'RatingSystemDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
